# Optional packages for the richer local-AI stack.
# Install with: pip install -r requirements.txt -r requirements-ai.txt

PyMuPDF>=1.26
sentence-transformers>=3.4
faiss-cpu>=1.9,<1.11
spacy>=3.8

# NOTE: llama-cpp-python is NOT included here because it requires a C++ compiler
# and takes 30-60 minutes to build from source on CI runners.
# For local development, install it separately:
#   pip install llama-cpp-python>=0.3
